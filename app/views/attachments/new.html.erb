<div class="page-header">
  <%= link_to artefact_attachments_path, :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <h1>New attachment for <%= @artefact.name%></h1>
</div>

<%= render 'form' %>
